# UTKFace White vs Non-White 이진 실험 코드 변경 보고서

- **작성일**: 2026-08-17
- **브랜치**: `feature/utkface-multigroup-race` (마지막 커밋 `8a3002d1` 이후의 **미커밋 작업분** 중 이진 실험 경로 관련 변경만 정리)

## 1. 변경 요약

| 파일 | 내용 |
|---|---|
| `dataset.py` | `race_binary` 이진 병합, (gender×race5) 층화 8:1:1 분할, FSCL식 편향 주입, 평가셋 균형화 |
| `main_test.py` | `--race_binary` / `--skew_beta` 플래그와 가드, EO 계산 이진화 |
| `train_and_val.py` | EO 계산 이진화 (main_test와 동일 기준) |
| `scripts/make_utkface_full_csv.py` | 신규 — Others 포함 전체 라벨 CSV 생성 |
| `tests/test_utkface_binary.py` | 신규 — 이진 병합·층화·skew 유닛테스트 5종 |
| `csv/UTKFace_labels_full.csv`, `csv/utkface_split_seed42.csv` | 신규 — 전체 라벨(23,703행) / 층화 분할 결과 |

목표: FSCL(CVPR'22)의 "Caucasian or not" 세팅을 재현해, **민감그룹 = White(0) vs Non-White(1)**, **태스크 = gender(또는 age 이진)** 로 Phase 1(자연 편향) → Phase 2(편향 주입) 실험을 돌릴 수 있는 경로를 추가.

---

## 2. 전체 라벨 CSV (`scripts/make_utkface_full_csv.py` → `csv/UTKFace_labels_full.csv`)

기존 `csv/UTKFace_labels.csv`(22,013행)에는 Others 인종이 빠져 있었습니다. FSCL의 "Caucasian or not" 정의를 재현하려면 Others도 Non-White에 포함해야 하므로, UTKFace 파일명에서 라벨을 직접 파싱해 **Others 1,692장을 포함한 23,703행** CSV를 새로 만들었습니다(파일명 형식이 깨진 5장은 스킵). 기존 CSV는 그대로 보존해 기존 경로 회귀에 영향이 없습니다.

## 3. `dataset.relabel(race_binary=)` — White(0) vs Non-White(1) 병합

```python
def relabel(frame, seven_races=True, drop_race=False, race_binary=False):

    # race_binary는 White vs Non-White 병합, drop_race는 특정 인종 삭제 — 의미가 충돌한다.
    if race_binary and drop_race:
        raise ValueError("race_binary와 drop_race는 동시에 사용할 수 없습니다.")
    ...
    frame.loc[frame['race'] == 'Others', 'race'] = 7  # UTKFace 전용 (csv/UTKFace_labels_full.csv)
    ...
    # White vs Non-White 이진 병합 (FSCL 'Caucasian or not' 정의, Others 포함).
    # 층화 분할·소그룹 분석용으로 병합 전 라벨을 race_orig에 보존한다.
    if race_binary and 'race' in frame.columns:
        frame['race_orig'] = frame['race']
        frame['race'] = (frame['race'] != 0).astype(int)

    # 7개 인종이 아닌 경우, race 라벨을 합치거나 제거 (race_binary 시에는 이미 병합됐으므로 건너뜀)
    if not seven_races and not race_binary and 'race' in frame.columns:
        ...
```

- `race`가 **White=0 / Non-White=1** 이진이 되고, 병합 전 5범주(0=White, 1=Black, 3=East Asian, 5=Indian, 7=Others)는 `race_orig`에 보존됩니다(층화 분할·소그룹 분석용).
- `race_binary`와 `drop_race`(기존 커밋의 다중 인종 제거 기능)는 의미가 충돌하므로 동시 사용을 막았습니다.
- 기존 `not seven_races` 4범주 병합 분기는 `race_binary`가 아닐 때만 타도록 조건을 추가했습니다.
- `make_frame`의 라벨 정수 dtype 보장 대상에 `race_orig`를 추가했습니다.

## 4. `make_frame(stratify=, split_csv_out=)` — (gender × race5) 층화 8:1:1 분할

기존에는 이미지 이름 전체를 시드 42로 섞어 8:1:1로 잘랐는데(단순 무작위), 이진 실험에서는 소그룹(예: Others 여성) 비율이 분할마다 흔들릴 수 있어 **셀 단위 층화 분할**을 추가했습니다.

```python
if stratify:
    # (gender × 원본 race) 층화 무작위 8:1:1 분할. 셀별로 seed 42 셔플 후 비율대로 나눠
    # 3분할 모두에서 소그룹 비율을 보존한다 (race_binary 시 race_orig=병합 전 5범주 기준).
    # 행 단위 분할이므로 이미지당 얼굴이 1개(행:이미지 = 1:1)인 데이터셋 전용 (UTKFace 등).
    if image_name_frame.nunique() != frame.shape[0]:
        raise ValueError("stratify=True는 이미지당 1행인 데이터셋에서만 사용할 수 있습니다.")
    race_col = 'race_orig' if 'race_orig' in frame.columns else 'race'
    strat_cols = [c for c in ['gender', race_col] if c in frame.columns]
    rng = np.random.RandomState(42)
    split = pd.Series('train', index=frame.index)
    for _, cell_idx in sorted(frame.groupby(strat_cols).groups.items()):
        cell_idx = rng.permutation(np.asarray(cell_idx))
        n = len(cell_idx)
        n_tr = int(train_pct * n)
        n_v = int((n - n_tr) / 2)
        split.loc[cell_idx[n_tr:n_tr + n_v]] = 'val'
        split.loc[cell_idx[n_tr + n_v:]] = 'test'

    train_frame = frame[split == 'train'].reset_index(drop=True)
    val_frame = frame[split == 'val'].reset_index(drop=True)
    test_frame = frame[split == 'test'].reset_index(drop=True)
```

- 층화 기준은 병합 **전** 5범주(`race_orig`) × gender → 10셀. 3분할 각각의 셀 비율이 전체와 근사한지 검증 리포트를 출력합니다.
- `split_csv_out`을 주면 분할 결과를 `csv/utkface_split_seed42.csv`(image_name, split)로 저장해 재현·감사가 가능합니다.
- 행:이미지가 1:1이 아닌 데이터셋(FairFace 7종 등)에서 잘못 쓰이지 않게 가드를 넣었고, `stratify=False`(기본)면 기존 분할 로직 그대로입니다.

## 5. Phase 2 편향 주입 — `apply_fscl_skew(beta)` / `make_balanced_eval`

자연 편향이 작을 때(repo 문서 기준 EO≈0.066) FSCL 프로토콜처럼 **train에만 그룹×성별 상관을 인위 주입**하고, val/test는 완전 균형으로 만드는 함수를 추가했습니다.

```python
# FSCL(CVPR'22)식 편향 주입: train만 재표집해 그룹×성별 상관을 인위적으로 만든다.
# White(race=0)는 male:female = beta:1, Non-White(race=1)는 1:beta.
# gender 코딩은 relabel 기준 0=Male, 1=Female. 데이터 추가는 불가하므로 다수 쪽을 다운샘플한다.
def apply_fscl_skew(train_frame, beta, seed=42):
    if beta <= 1:
        raise ValueError(f"skew 비율 beta는 1보다 커야 합니다. 현재: {beta}")
    if not set(train_frame['race'].unique()) <= {0, 1}:
        raise ValueError("apply_fscl_skew는 race ∈ {0,1} (race_binary) 프레임 전용입니다.")

    rng = np.random.RandomState(seed)
    parts = []
    for r in sorted(train_frame['race'].unique()):
        males = train_frame[(train_frame['race'] == r) & (train_frame['gender'] == 0)]
        females = train_frame[(train_frame['race'] == r) & (train_frame['gender'] == 1)]
        target = beta if r == 0 else 1.0 / beta  # 목표 male:female 비율
        if len(males) > target * len(females):
            n_m, n_f = int(round(target * len(females))), len(females)
        else:
            n_m, n_f = len(males), int(round(len(males) / target))
        parts.append(males.iloc[rng.permutation(len(males))[:n_m]])
        parts.append(females.iloc[rng.permutation(len(females))[:n_f]])
        print(f"[skew β={beta}] race={r}: male {len(males)}→{n_m}, female {len(females)}→{n_f} "
              f"(m:f = {n_m / max(n_f, 1):.2f})")

    out = pd.concat(parts)
    out = out.iloc[rng.permutation(len(out))].reset_index(drop=True)
    return out
```

```python
# 평가셋 균형화: (race × gender) 각 셀을 최소 셀 크기로 다운샘플해 완전 균형으로 만든다.
# FSCL 프로토콜의 'val/test는 그룹×클래스 완전 균형' 재현용 (Phase 2 전용).
def make_balanced_eval(frame, seed=42):
    rng = np.random.RandomState(seed)
    cell_sizes = frame.groupby(['race', 'gender']).size()
    n_min = int(cell_sizes.min())
    parts = []
    for _, sub in frame.groupby(['race', 'gender']):
        parts.append(sub.iloc[rng.permutation(len(sub))[:n_min]])
    out = pd.concat(parts).reset_index(drop=True)
    print(f"[balanced eval] 셀 크기:\n{cell_sizes.to_string()}\n→ 셀당 {n_min}개, 총 {out.shape[0]}행")
    return out
```

## 6. `main_test.py` — 플래그·가드·적용 순서

### 6-1. CLI 플래그 추가

```python
parser.add_argument('--race_binary', action='store_true',
    help='UTKFace 전용: race를 White(0) vs Non-White(1) 이진으로 병합 '
         '(Others 포함, csv/UTKFace_labels_full.csv + gender×race5 층화 분할 사용)')
parser.add_argument('--skew_beta', type=float, default=0,
    help='UTKFace race_binary 전용: FSCL식 편향 주입 비율 β (>1). '
         'train을 White m:f=β:1 / Non-White 1:β로 재표집하고 val/test는 (race×gender) 4셀 균형으로 재구성. 0이면 비활성')
```

### 6-2. 데이터 경로·가드

```python
# race_binary(White vs Non-White) 모드 가드: race 태스크·drop_race와는 의미가 충돌한다.
if race_binary and loss_type not in ('gender', 'age'):
    raise ValueError(f"--race_binary는 loss_type gender/age에서만 사용할 수 있습니다. 현재: {loss_type}")
if race_binary and drop_race:
    raise ValueError("--race_binary와 --drop_race는 동시에 사용할 수 없습니다.")
if race_binary:
    # Others 1,692장을 포함한 전체 CSV (scripts/make_utkface_full_csv.py 로 생성)
    csv = 'csv/UTKFace_labels_full.csv'
...
# race_binary 시에는 (gender × race 5범주) 층화 8:1:1 분할 + 분할 결과 CSV 저장.
frames = make_frame(csv, face_dir, seven_races=False, drop_race=drop_race,
                    race_binary=race_binary, stratify=race_binary,
                    split_csv_out='csv/utkface_split_seed42.csv' if race_binary else None)
```

### 6-3. Phase 2 skew 적용 (가독성 필터링 **이후**)

```python
# Phase 2 (FSCL식 편향 주입): 가독성 필터링 이후에 적용해 최종 데이터 기준으로 셀 비율을 맞춘다.
# train은 White m:f=β:1 / Non-White 1:β 재표집, val/test는 (race×gender) 4셀 완전 균형.
if skew_beta:
    if dataset != 'UTKFace' or not race_binary:
        raise ValueError("--skew_beta는 UTKFace + --race_binary 조합에서만 사용할 수 있습니다.")
    frames['train'] = apply_fscl_skew(frames['train'], skew_beta)
    frames['val'] = make_balanced_eval(frames['val'])
    frames['test'] = make_balanced_eval(frames['test'])
```

skew 적용을 dlib 가독성 필터링 이후에 둔 것이 포인트입니다. 필터링 전에 적용하면 이후 이미지 탈락으로 셀 비율이 다시 틀어집니다.

## 7. EO 계산 정의 변경: worst-group max−min → 이진 그룹 |차이|

`main_test.py`(테스트 평가)와 `train_and_val.py`(재학습 중 검증) 두 곳 모두 동일하게 변경했습니다.

```python
# (변경 전) EO 계산: 유효(표본 있는) 그룹만으로 max-min (worst-group)
valid_tpr = [v for v in tpr_vals if v is not None]
valid_fpr = [v for v in fpr_vals if v is not None]
if len(valid_tpr) >= 2 and len(valid_fpr) >= 2:
    tpr_gap = max(valid_tpr) - min(valid_tpr)
    fpr_gap = max(valid_fpr) - min(valid_fpr)

# (변경 후) EO 계산: 이진 그룹(0/1) 전제 — 두 그룹 TPR·FPR 차이의 절댓값
if (len(tpr_vals) >= 2
        and None not in (tpr_vals[0], tpr_vals[1], fpr_vals[0], fpr_vals[1])):
    tpr_gap = abs(tpr_vals[0] - tpr_vals[1])
    fpr_gap = abs(fpr_vals[0] - fpr_vals[1])
    eo = (tpr_gap + fpr_gap) / 2
```

- 이진 실험(W vs NW)에서는 두 정의가 수치상 같지만, **정의를 "그룹 0/1 TPR·FPR 차이"로 고정**해 문헌(FSCL 등)의 EO와 직접 비교 가능하게 했습니다.
- 종전에는 그룹 0에 표본이 없어도 나머지 그룹만으로 max−min을 계산했지만, 이제 그룹 0/1 중 한쪽이라도 표본이 없으면 EO를 N/A 처리합니다.
- 민감그룹이 3개 이상인 세팅으로 실행하면 그룹 2+는 EO에 반영되지 않는다는 경고를 출력합니다:

```python
if len(sensitive_groups) != 2:
    print(f"⚠ EO는 이진 그룹(0/1) 기준으로 계산됩니다. 현재 민감그룹 수={len(sensitive_groups)} — 그룹 2 이상은 EO에 반영되지 않습니다.")
```

## 8. 검증 상태

- 유닛테스트 5종(`tests/test_utkface_binary.py`): 이진 병합·race_orig 보존·층화 비율·skew 비율·평가셋 균형 검증 통과.
- `race_binary` 미사용 기본 경로 3종은 수정 전후 **회귀 완전 동일** 확인.
- φ_k 스모크: 그룹 {0,1}로 전 레이어 gap 계산 완주 확인.
- EO 로그(V6)·CelebA GPU 회귀는 첫 실제 실행에서 확인 예정.

## 9. 실행 커맨드

```bash
# Phase 1: 자연 편향 (White vs Non-White, task=gender)
python main_test.py --dataset UTKFace --network mobilenetv2 --loss_type gender \
    --sensitive_group race --race_binary --no_grl --impt 2

# Phase 2: FSCL식 편향 주입 (자연 편향이 작아 게이트 실패 시)
python main_test.py --dataset UTKFace --network mobilenetv2 --loss_type gender \
    --sensitive_group race --race_binary --skew_beta 3 --no_grl --impt 2
```
